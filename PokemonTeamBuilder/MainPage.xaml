<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PokemonTeamBuilder.MainPage">

    <ScrollView>
        <Grid Padding="15" RowSpacing="20" ColumnSpacing="20">

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <VerticalStackLayout Grid.Row="0" Grid.ColumnSpan="2" Spacing="10">

                <SearchBar x:Name="searchBar"
                           Placeholder="Search items..."
                           TextChanged="searchBar_TextChanged"
                           SearchButtonPressed="OnSearchBarPressed"/>

                <ListView x:Name="suggestionListView"
                          ItemSelected="suggestionListView_ItemSelected"
                          HeightRequest="100"
                          IsVisible="True"/>

            </VerticalStackLayout>

            <VerticalStackLayout Grid.Row="1" Grid.Column="0" Spacing="25" Padding="10" HorizontalOptions="CenterAndExpand">

                <Image x:Name="pokemonSprite"
                       WidthRequest="200"
                       HeightRequest="200"
                       HorizontalOptions="Center" />

                <Label x:Name="pokemonNameLabel"
                       FontSize="20"
                       FontAttributes="Bold"
                       HorizontalOptions="Center" />

            </VerticalStackLayout>

            <VerticalStackLayout Grid.Row="1" Grid.Column="1" Spacing="25" Padding="10">

                <Label x:Name="pokemonHeightLabel"
                       FontSize="15" />

                <Label x:Name="pokemonWeightLabel"
                       FontSize="15" />

                <Label x:Name="pokemonTypeLabel"
                       FontSize="15" />

                <Label x:Name="pokemonStatLabel"
                       FontSize="15" />

                <Button x:Name="pokemonFavouriteButton"
                        IsVisible="False"
                        FontSize="15"
                        Text=""
                        Clicked="OnFavouriteClicked"
                        HorizontalOptions="Start"
                        Margin="0, 10, 0, 0" />

            </VerticalStackLayout>



        </Grid>
    </ScrollView>

</ContentPage>
