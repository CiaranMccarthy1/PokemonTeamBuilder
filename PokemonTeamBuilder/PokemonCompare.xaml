<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PokemonTeamBuilder.PokemonCompare"
             Title="Compare Pokémon">

    <Grid Padding="16" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <VerticalStackLayout Grid.Row="0" Spacing="8">
            <Label Text="Compare Pokémon"
                   FontSize="28"
                   FontAttributes="Bold" />
            <Label Text="Select 2-4 Pokémon to compare their stats side-by-side"
                   FontSize="14"
                   TextColor="{DynamicResource SubTextColor}" />
        </VerticalStackLayout>

        <!-- Search Bar and Buttons -->
        <Frame Grid.Row="1" Padding="16" CornerRadius="12" HasShadow="False"
               BackgroundColor="{DynamicResource CardBackgroundColor}"
               BorderColor="{DynamicResource BorderColor}">
            <Grid ColumnSpacing="12" RowSpacing="{OnIdiom Phone=12, Default=0}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="{OnIdiom Phone=0, Default=Auto}" />
                    <ColumnDefinition Width="{OnIdiom Phone=0, Default=Auto}" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="{OnIdiom Phone=Auto, Default=0}" />
                </Grid.RowDefinitions>

                <!-- SearchBar takes full width on mobile -->
                <Frame Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="{OnIdiom Phone=3, Default=1}" 
                       Padding="0" CornerRadius="20" HasShadow="False"
                       BackgroundColor="{DynamicResource SurfaceColor}"
                       BorderColor="{DynamicResource BorderColor}">
                    <SearchBar x:Name="searchBar"
                               Placeholder="Search Pokémon to add..."
                               TextChanged="OnSearchTextChanged"
                               SearchButtonPressed="OnAddPokemonClicked"
                               BackgroundColor="Transparent" />
                </Frame>

                <!-- Buttons stack on second row on mobile, side by side on desktop -->
                <Button Grid.Row="{OnIdiom Phone=1, Default=0}" 
                        Grid.Column="{OnIdiom Phone=0, Default=1}"
                        Grid.ColumnSpan="{OnIdiom Phone=1, Default=1}"
                        Text="+ Add"
                        Clicked="OnAddPokemonClicked"
                        BackgroundColor="{DynamicResource AccentColor}"
                        HorizontalOptions="{OnIdiom Phone=Fill, Default=Center}"
                        Padding="16,8" />

                <Button Grid.Row="{OnIdiom Phone=1, Default=0}"
                        Grid.Column="{OnIdiom Phone=1, Default=2}"
                        Grid.ColumnSpan="{OnIdiom Phone=1, Default=1}"
                        Text="Clear All"
                        Clicked="OnClearAllClicked"
                        BackgroundColor="{DynamicResource SecondaryColor}"
                        HorizontalOptions="{OnIdiom Phone=Fill, Default=Center}"
                        Padding="16,8" />
            </Grid>
        </Frame>

        <!-- Suggestions List-->
        <Frame x:Name="suggestionsFrame" Grid.Row="2" Padding="0" CornerRadius="12" HasShadow="False"
               IsVisible="False"
               BackgroundColor="{DynamicResource CardBackgroundColor}"
               BorderColor="{DynamicResource BorderColor}">
            <ListView x:Name="suggestionListView"
                      ItemSelected="OnSuggestionSelected"
                      HeightRequest="150"
                      BackgroundColor="Transparent"
                      SeparatorColor="{DynamicResource DividerColor}" />
        </Frame>

        <!-- Comparison-->
        <ScrollView Grid.Row="3">
            <VerticalStackLayout Spacing="16">

                <Frame Padding="16" CornerRadius="12" HasShadow="False"
                       BackgroundColor="{DynamicResource CardBackgroundColor}"
                       BorderColor="{DynamicResource BorderColor}">
                    <VerticalStackLayout Spacing="12">
                        <Label Text="Selected Pokémon" FontSize="18" FontAttributes="Bold" />
                        <!-- Wrap in ScrollView for horizontal scrolling on mobile -->
                        <ScrollView Orientation="Horizontal" HorizontalScrollBarVisibility="Always">
                            <HorizontalStackLayout x:Name="selectedPokemonContainer" 
                                                   Spacing="12" 
                                                   Padding="0,0,12,0" />
                        </ScrollView>
                        <Label x:Name="selectionHintLabel"
                               Text="Add 2-4 Pokémon to compare"
                               FontSize="14"
                               TextColor="{DynamicResource SubTextColor}"
                               HorizontalOptions="Center" />
                    </VerticalStackLayout>
                </Frame>

                <Frame x:Name="comparisonFrame" Padding="20" CornerRadius="12" HasShadow="False"
                       IsVisible="False"
                       BackgroundColor="{DynamicResource CardBackgroundColor}"
                       BorderColor="{DynamicResource BorderColor}">
                    <VerticalStackLayout Spacing="16">
                        <Label Text="📊 Stats Comparison" FontSize="20" FontAttributes="Bold" />
                        <HorizontalStackLayout x:Name="legendContainer" Spacing="16" HorizontalOptions="Center" />
                        <BoxView HeightRequest="1" Color="{DynamicResource DividerColor}" />
                        <VerticalStackLayout x:Name="statsComparisonContainer" Spacing="20" />
                        <BoxView HeightRequest="1" Color="{DynamicResource DividerColor}" />
                        <VerticalStackLayout x:Name="totalStatsContainer" Spacing="8" />
                    </VerticalStackLayout>
                </Frame>
                
                <Frame x:Name="typeComparisonFrame" Padding="20" CornerRadius="12" HasShadow="False"
                       IsVisible="False"
                       BackgroundColor="{DynamicResource CardBackgroundColor}"
                       BorderColor="{DynamicResource BorderColor}">
                    <VerticalStackLayout Spacing="12">
                        <Label Text="🏷️ Type Comparison" FontSize="20" FontAttributes="Bold" />
                        <Grid x:Name="typeComparisonGrid" ColumnSpacing="16" RowSpacing="8" />
                    </VerticalStackLayout>
                </Frame>

            </VerticalStackLayout>
        </ScrollView>
    </Grid>

</ContentPage>